{
  "name": "chrome-extension-manhour",
  "version": "0.1.1",
  "description": "This is an extension feature that assists with input in Jobcan's Man Hour Management.",
  "private": true,
  "scripts": {
    "build": "tsc && vite build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bootstrap": "^5.2.10",
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.2",
    "bootstrap": "^5.3.3",
    "glob": "^11.0.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
